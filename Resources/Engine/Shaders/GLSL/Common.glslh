struct ObjectData
{
    mat4 transform;
    vec4 sphereBounds;
};

struct DirectionalLight
{
    vec4 direction;
    vec4 colorIntensity;
};

struct CameraData
{
    mat4 view;
    mat4 proj;
    mat4 viewProj;

    vec4 position;
};

struct TargetData
{
    vec2 targetSize;
};

struct PassData
{
    uint passIndex;
};